import React from "react";

const NotFound = () => {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        margin: "auto",
        minHeight: "500px",
      }}
    >
      <h1 style={{ fontSize: "100px" }}>404 not found</h1>
    </div>
  );
};

export default NotFound;
